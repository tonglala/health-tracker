# Netlify 部署指南（替代方案）

由於 Vercel 在載入 CSS 時遇到問題，我們改用 Netlify 部署。

## 🚀 部署步驟

### 1. 前往 Netlify

打開瀏覽器，前往：**https://app.netlify.com**

### 2. 登入

- 點擊 **`Log in`** 或 **`Sign up`**
- 選擇 **`GitHub`** 登入
- 授權 Netlify 存取你的 GitHub

### 3. 匯入專案

1. 登入後，點擊 **`Add new site`** → **`Import an existing project`**
2. 選擇 **`Deploy with GitHub`**
3. 找到並點擊 **`health-tracker`** repository
4. 設定頁面：
   - **Branch to deploy**: `main`
   - **Build command**: 留空（不填）
   - **Publish directory**: `.` （一個點）
5. 點擊 **`Deploy site`**

### 4. 等待部署

- 部署時間約 30 秒
- 完成後會顯示網址，格式類似：`https://xxx.netlify.app`

### 5. 測試網站

- 複製 Netlify 給你的網址
- 在手機瀏覽器開啟
- 這次應該會看到完整的樣式和功能！

---

## 📱 使用方式

與 Vercel 相同：
1. 複製網址
2. 在手機開啟
3. 加入主畫面當作 App

---

## 🔄 未來更新

修改程式碼後：
```powershell
git add .
git commit -m "更新說明"
git push
```

Netlify 會自動重新部署（約 30 秒）。
